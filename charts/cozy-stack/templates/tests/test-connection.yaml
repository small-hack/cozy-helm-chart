apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "cozy-stack.fullname" . }}-test-connection"
  labels:
    {{- include "cozy-stack.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "cozy-stack.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
